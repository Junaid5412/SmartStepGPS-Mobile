<?xml version="1.0" encoding="utf-8"?>
<!-- Splash screen for Android 11 and below, drawn by the window manager before Flutter has started.
     This used to be the untouched Flutter template: a plain white window. The app then faded from
     white, to its own Dart splash, to the UI - two visible jumps on every cold start.
     Android 12 and above ignore this and use values-v31/styles.xml instead. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
